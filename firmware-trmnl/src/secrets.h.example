#pragma once
// Copy to src/secrets.h (gitignored) and fill in. All optional — values can
// instead be provisioned into NVS at runtime (namespaces "grafana-trmnlx" for
// Wi-Fi, "trmnlx-cfg" for worker_url/bearer).

#define DEFAULT_WIFI_SSID_0     "HomeWiFi"
#define DEFAULT_WIFI_PASSWORD_0 "..."

// #define DEFAULT_WIFI_SSID_1     "SecondNetwork"
// #define DEFAULT_WIFI_PASSWORD_1 "..."

#define DEFAULT_WORKER_URL      "https://dashboard.contexa.net/bundle-trmnl"
#define DEFAULT_WORKER_BEARER   "<your-worker-bearer-token>"

// #define ENABLE_TOUCH 0   // disable touch-wake, pure timed rotation
