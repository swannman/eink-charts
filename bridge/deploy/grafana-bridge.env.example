# /etc/default/grafana-bridge
# Owned root:grafana-bridge, mode 0640.

GRAFANA_TOKEN=glsa_REPLACE_ME
GRAFANA_BRIDGE_CONFIG=/etc/grafana-bridge/config.yaml
LISTEN_HOST=0.0.0.0
LISTEN_PORT=8080
LOG_LEVEL=INFO
