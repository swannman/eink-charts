# Partition layout for X3 (16MB flash). Two OTA app slots for safe OTA;
# NVS sized to hold the panel-bundle cache + WiFi creds with headroom as
# the dashboard grows (~1.3 KB per panel × 3 views).
# Name,    Type, SubType,  Offset,    Size,     Flags
nvs,       data, nvs,      0x9000,    0x15000,
otadata,   data, ota,      0x1e000,   0x2000,
app0,      app,  ota_0,    0x20000,   0x300000,
app1,      app,  ota_1,    0x320000,  0x300000,
spiffs,    data, spiffs,   0x620000,  0x9E0000,
